program ex14;

var
    inp,out: text;

begin 
    assign(inp,'input.inp');
    assign(out,'output.out');
    Reset(inp);
    Rewrite(out);
    setLength(a,0);
    while not eoln(inp) do
    begin
        
    end; 

end.